import type { CertificateTemplate } from "../types";

// §10.9 — placeholder for any not-yet-transcribed formats. All 12 are now transcribed
// (i–xii), so this is empty. If ICAI adds a format, scaffold it here with its sourcePages
// and no segments; the verifier skips draft templates that have no locked text yet.
export const drafts: CertificateTemplate[] = [];
